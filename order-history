<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
  <title>Order History - Snehvastra</title>
  <meta name="description" content="View your past Snehvastra orders, their statuses, and details in one elegant dark-glass dashboard." />
  <style>
    :root {
      --bg-light-1: #FCFAF6;
      --bg-light-2: #FAF6E7;
      --text-dark: #4B4337;
      --muted: rgba(75, 67, 55, 0.7);
      --accent-gradient: linear-gradient(90deg, #BFA658, #ECD78F);
      --panel-bg: rgba(252, 250, 246, 0.05);
      --panel-border: rgba(191, 166, 88, 0.1);
      --shadow-light: rgba(191, 166, 88, 0.05);
      --max-width: 900px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: linear-gradient(180deg, var(--bg-light-1), var(--bg-light-2));
      color: var(--text-dark);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.2rem 5%;
      background: var(--panel-bg);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--panel-border);
      box-shadow: 0 0 20px var(--shadow-light);
      max-width: var(--max-width);
      margin: 0 auto;
      border-radius: 16px 16px 0 0;
    }

    .logo {
      height: 45px;
      user-select: none;
      filter: drop-shadow(0 0 10px var(--shadow-light));
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.5rem;
      flex-wrap: wrap;
    }

    nav a {
      text-decoration: none;
      color: var(--muted);
      font-weight: 500;
      transition: color 0.3s;
      padding: 8px 12px;
      border-radius: 8px;
    }

    nav a:hover,
    nav a.active {
      color: var(--accent-gradient);
      background: rgba(191, 166, 88, 0.1);
      text-shadow: 0 0 12px rgba(191, 166, 88, 0.6);
    }

    main {
      flex: 1;
      padding: 3rem 5% 4rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      animation: fadeIn 0.8s ease;
      max-width: var(--max-width);
      margin: 0 auto;
      user-select: text;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 2rem;
      text-align: center;
      letter-spacing: 0.5px;
      background: var(--accent-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      user-select: none;
    }

    table {
      width: 90%;
      max-width: var(--max-width);
      border-collapse: collapse;
      background: var(--panel-bg);
      backdrop-filter: blur(20px);
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 0 30px var(--shadow-light);
      user-select: none;
    }

    thead {
      background: rgba(191, 166, 88, 0.05);
    }

    th,
    td {
      padding: 1rem 1.2rem;
      text-align: left;
      border-bottom: 1px solid rgba(191, 166, 88, 0.05);
      font-size: 0.95rem;
      color: var(--text-dark);
      user-select: text;
    }

    th {
      color: var(--muted);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      user-select: none;
    }

    tbody tr {
      transition: background 0.3s, transform 0.3s;
    }

    tbody tr:hover {
      background: rgba(191, 166, 88, 0.08);
      transform: scale(1.01);
    }

    .status {
      font-weight: 600;
      text-transform: capitalize;
      text-align: center;
      padding: 0.4rem 0.8rem;
      border-radius: 10px;
      color: var(--muted);
      position: relative;
    }

    .status::before {
      content: '';
      display: inline-block;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      margin-right: 6px;
      vertical-align: middle;
      background-color: currentColor;
    }

    .status.completed {
      color: #7CFC00;
    }

    .status.shipped {
      color: #00CED1;
    }

    .status.processing {
      color: #FFD700;
    }

    footer {
      text-align: center;
      padding: 1.5rem 0;
      font-size: 0.9rem;
      color: var(--muted);
      background: rgba(252, 250, 246, 0.85);
      border-top: 1px solid var(--panel-border);
      backdrop-filter: blur(8px);
      user-select: none;
      max-width: var(--max-width);
      margin: 0 auto;
      border-radius: 0 0 16px 16px;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(15px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>
  <header>
    <img src="images/logo.final.png" alt="Snehvastra Logo" class="logo" />
    <nav>
      <ul>
        <li><a href="index.html">Home</a></li>
        <li><a href="collection.html">Collection</a></li>
        <li><a href="about.html">About</a></li>
        <li><a href="contact.html">Contact</a></li>
        <li><a href="wishlist.html">Wishlist</a></li>
        <li><a href="cart.html">Cart</a></li>
        <li><a href="user-login.html">Login</a></li>
      </ul>
    </nav>
  </header>

  <main>
    <h1>Your Order History</h1>
    <table aria-label="Order history table">
      <thead>
        <tr>
          <th>Order ID</th>
          <th>Date</th>
          <th>Items</th>
          <th>Total</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>#1001</td>
          <td>2025-09-15</td>
          <td>3</td>
          <td>₹8,499</td>
          <td class="status completed">Completed</td>
        </tr>
        <tr>
          <td>#1002</td>
          <td>2025-09-29</td>
          <td>1</td>
          <td>₹3,499</td>
          <td class="status shipped">Shipped</td>
        </tr>
        <tr>
          <td>#1003</td>
          <td>2025-10-02</td>
          <td>2</td>
          <td>₹5,999</td>
          <td class="status processing">Processing</td>
        </tr>
      </tbody>
    </table>
  </main>

  <footer>
    &copy; 2025 Snehvastra. All Rights Reserved.<br />
    Instagram: @sneh_vastra | Facebook: Snehvastra
  </footer>

  <!-- Google Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-CXQG6L0W01"></script>
  <script>
    window.dataLayer = window.dataLayer || [];

    function gtag() {
      dataLayer.push(arguments);
    }
    gtag('js', new Date());
    gtag('config', 'G-CXQG6L0W01');
  </script>
</body>

</html>
