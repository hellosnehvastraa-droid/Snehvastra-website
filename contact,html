<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
  <title>Contact Us - Snehvastra</title>
  <meta name="description" content="Contact Snehvastra artisans. Support, auctions, custom orders. Phone, WhatsApp, email, studio visit." />
  <meta name="theme-color" content="#ECD78F">

  <!-- FONTS -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Clash+Display:wght@500;600;700&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />

  <style>
    :root {
      --bg-light-1: #FCFAF6;
      --bg-light-2: #FAF6E7;
      --text-dark: #4B4337;
      --muted-gold: #BFA658;
      --highlight-gold: #ECD78F;
      --whatsapp-green: #25D366;
      --max-w: 1100px;
      --radius: 20px;
      --transition-speed: 0.35s;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }
    body {
      background: linear-gradient(180deg, var(--bg-light-1), var(--bg-light-2));
      color: var(--text-dark); min-height: 100vh; overflow-x: hidden; line-height: 1.7;
    }

    /* MASTER HEADER */
    header {
      background: rgba(252,250,246,0.95); display: flex; justify-content: space-between; align-items: center;
      padding: 20px 5%; position: sticky; top: 0; z-index: 100; max-width: var(--max-w); margin: 0 auto;
      border-bottom: 1px solid var(--muted-gold); border-radius: 0 0 var(--radius) var(--radius);
      backdrop-filter: blur(12px);
    }
    .logo { height: 50px; width: auto; filter: drop-shadow(0 0 12px var(--highlight-gold)); }
    nav ul { display: flex; list-style: none; gap: 15px; flex-wrap: wrap; }
    nav ul li a {
      color: var(--muted-gold); font-size: 0.9rem; font-weight: 600; padding: 8px 14px; border-radius: 8px;
      transition: all var(--transition-speed) ease; text-decoration: none;
    }
    nav ul li a:hover, nav ul li a.active { 
      color: var(--highlight-gold); background: rgba(236,215,143,0.15); 
    }
    #cartCount { 
      background: var(--highlight-gold); color: var(--text-dark); border-radius: 50%; 
      padding: 2px 6px; font-size: 0.8rem; font-weight: 700; margin-left: 4px;
    }

    .auction-banner {
      background: linear-gradient(135deg, var(--highlight-gold), var(--muted-gold));
      color: var(--text-dark); padding: 15px 20px; text-align: center; font-weight: 700;
      position: sticky; top: 92px; z-index: 90; box-shadow: 0 4px 20px rgba(236,215,143,0.3);
      margin: 0 5%; border-radius: var(--radius);
    }
    .auction-timer { 
      font-family: 'Clash Display', serif; font-size: 0.9rem; background: rgba(255,255,255,0.95); 
      padding: 6px 12px; border-radius: 12px; display: inline-block; margin-left: 8px; 
    }

    main { padding: 160px 5% 80px; max-width: var(--max-w); margin: 0 auto; }
    
    .contact-hero {
      text-align: center; background: rgba(255,255,255,0.95); padding: 80px 40px; 
      border-radius: var(--radius); border: 1px solid rgba(191,166,88,0.15);
      box-shadow: 0 30px 100px rgba(236,215,143,0.1); backdrop-filter: blur(20px);
      margin-bottom: 60px; position: relative; overflow: hidden;
    }
    .contact-hero::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 4px;
      background: linear-gradient(90deg, var(--muted-gold), var(--highlight-gold));
    }
    .contact-hero h1 {
      font-family: 'Clash Display', serif; font-size: clamp(3rem, 8vw, 4.5rem); font-weight: 700;
      background: linear-gradient(135deg, var(--muted-gold), var(--highlight-gold));
      -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin-bottom: 20px;
    }

    /* CONTACT OPTIONS GRID */
    .contact-grid {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 40px; margin-bottom: 60px;
    }
    .contact-card {
      background: rgba(255,255,255,0.95); padding: 40px 32px; border-radius: var(--radius);
      border: 1px solid rgba(191,166,88,0.15); box-shadow: 0 20px 60px rgba(236,215,143,0.1);
      backdrop-filter: blur(15px); text-align: center; transition: all var(--transition-speed);
      position: relative; overflow: hidden;
    }
    .contact-card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 4px;
      background: linear-gradient(90deg, var(--muted-gold), var(--highlight-gold));
      transform: scaleX(0); transition: transform var(--transition-speed);
    }
    .contact-card:hover::before { transform: scaleX(1); }
    .contact-card:hover { transform: translateY(-8px); box-shadow: 0 30px 80px rgba(236,215,143,0.2); }
    .contact-icon { font-size: 3.5rem; margin-bottom: 24px; display: block; }
    .whatsapp .contact-icon { color: var(--whatsapp-green); }
    .contact-card h3 {
      font-family: 'Clash Display', serif; font-size: 1.4rem; margin-bottom: 16px; color: var(--text-dark);
    }
    .contact-btn {
      display: inline-block; padding: 16px 32px; background: linear-gradient(135deg, var(--muted-gold), var(--highlight-gold));
      color: var(--text-dark); text-decoration: none; border-radius: 12px; font-weight: 600;
      transition: all var(--transition-speed); font-family: 'Clash Display', serif;
      box-shadow: 0 8px 25px rgba(191,166,88,0.3);
    }
    .contact-btn:hover { transform: translateY(-2px); box-shadow: 0 12px 35px rgba(191,166,88,0.5); }
    .whatsapp .contact-btn { background: var(--whatsapp-green); color: white; }

    /* CONTACT FORM */
    .contact-form-section {
      background: rgba(255,255,255,0.95); padding: 48px; border-radius: var(--radius);
      border: 1px solid rgba(191,166,88,0.15); box-shadow: 0 25px 80px rgba(236,215,143,0.1);
      backdrop-filter: blur(20px);
    }
    .contact-form-section h2 {
      font-family: 'Clash Display', serif; font-size: 2rem; text-align: center; margin-bottom: 40px;
      background: linear-gradient(135deg, var(--muted-gold), var(--highlight-gold));
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    }
    .form-grid {
      display: grid; grid-template-columns: 1fr 1fr; gap: 32px; margin-bottom: 32px;
    }
    .form-group { position: relative; }
    .form-group input, .form-group textarea, .form-group select {
      width: 100%; padding: 20px 24px; border: 2px solid rgba(191,166,88,0.2);
      border-radius: 12px; font-size: 1rem; background: rgba(255,255,255,0.9);
      transition: all var(--transition-speed); font-family: inherit;
    }
    .form-group input:focus, .form-group textarea:focus, .form-group select:focus {
      outline: none; border-color: var(--highlight-gold); box-shadow: 0 0 0 4px rgba(236,215,143,0.15);
    }
    .form-group textarea { resize: vertical; min-height: 140px; }
    .submit-btn {
      width: 100%; padding: 24px; background: linear-gradient(135deg, var(--muted-gold), var(--highlight-gold));
      border: none; border-radius: 16px; color: var(--text-dark); font-weight: 700; font-size: 1.2rem;
      cursor: pointer; transition: all var(--transition-speed); font-family: 'Clash Display', serif;
      box-shadow: 0 16px 50px rgba(191,166,88,0.4);
    }
    .submit-btn:hover { transform: translateY(-4px); box-shadow: 0 24px 60px rgba(191,166,88,0.6); }

    /* STUDIO MAP */
    .studio-map {
      height: 400px; border-radius: var(--radius); overflow: hidden; margin-top: 60px;
      box-shadow: 0 20px 60px rgba(236,215,143,0.15); border: 1px solid rgba(191,166,88,0.1);
    }

    /* RESPONSIVE */
    @media (max-width: 768px) {
      .form-grid { grid-template-columns: 1fr; }
      header { flex-direction: column; gap: 20px; padding: 18px 20px !important; }
      nav ul { gap: 10px; justify-content: center; }
      main { padding: 140px 18px 40px !important; }
      .contact-grid { grid-template-columns: 1fr; }
    }

    /* WHATSAPP FLOAT + TOAST */
    .whatsapp-float {
      position: fixed; bottom: 30px; right: 30px; width: 70px; height: 70px; border-radius: 50%;
      background: var(--whatsapp-green); display: flex; align-items: center; justify-content: center;
      box-shadow: 0 10px 40px rgba(37,211,102,0.4); z-index: 1000; cursor: pointer;
      transition: all var(--transition-speed); font-size: 2rem; color: white;
    }
    .whatsapp-float:hover { transform: scale(1.1); box-shadow: 0 15px 50px rgba(37,211,102,0.6); }
    
    .toast {
      position: fixed; top: 120px; right: 20px; background: linear-gradient(135deg, var(--highlight-gold), var(--muted-gold));
      color: var(--text-dark); padding: 16px 24px; border-radius: 12px; box-shadow: 0 10px 35px rgba(236,215,143,0.4);
      font-weight: 600; z-index: 1000; transform: translateX(400px); transition: transform 0.3s ease;
      max-width: 350px;
    }
    .toast.show { transform: translateX(0); }
  </style>
</head>

<body>
  <!-- HEADER -->
  <header>
    <img src="images/logo.final.png" alt="Snehvastra Logo" class="logo" />
    <nav>
      <ul>
        <li><a href="index.html">Home</a></li>
        <li><a href="collection.html">Collection</a></li>
        <li><a href="contact.html" class="active">Contact</a></li>
        <li><a href="auction.html">Auction</a></li>
        <li><a href="cart.html">Cart (<span id="cartCount">0</span>)</a></li>
      </ul>
    </nav>
  </header>

  <!-- AUCTION BANNER -->
  <div class="auction-banner">
    🔥 SNEHVASTRA ONE AUCTION <div class="auction-timer" id="auctionTimer">Loading...</div>
  </div>

  <main>
    <!-- HERO -->
    <div class="contact-hero">
      <h1>Get In Touch</h1>
      <p style="font-size: 1.3rem; color: rgba(75,67,55,0.85); max-width: 700px; margin: 0 auto;">
        We're artisans who craft with love. Reach out for custom orders, auction questions, or studio visits.
      </p>
    </div>

    <!-- CONTACT OPTIONS -->
    <div class="contact-grid">
      <div class="contact-card whatsapp">
        <div class="contact-icon">💬</div>
        <h3>WhatsApp Us</h3>
        <p style="color: rgba(75,67,55,0.85); margin-bottom: 24px;">Instant support 10AM-8PM</p>
        <a href="https://wa.me/917597152209?text=Hi%20Snehvastra!%20I%20have%20a%20question..." class="contact-btn">Message Now</a>
      </div>
      <div class="contact-card">
        <div class="contact-icon">📞</div>
        <h3>Call Us</h3>
        <p style="color: rgba(75,67,55,0.85); margin-bottom: 24px;">+91 75971 52209</p>
        <p style="font-size: 0.9rem; color: var(--muted-gold);">10AM - 7PM IST (Mon-Sat)</p>
      </div>
      <div class="contact-card">
        <div class="contact-icon">✉️</div>
        <h3>Email Support</h3>
        <p style="color: rgba(75,67,55,0.85); margin-bottom: 24px;">support@snehvastra.store</p>
        <p style="font-size: 0.9rem; color: var(--muted-gold);">Reply within 24 hours</p>
      </div>
      <div class="contact-card">
        <div class="contact-icon">🏛️</div>
        <h3>Visit Studio</h3>
        <p style="color: rgba(75,67,55,0.85); margin-bottom: 24px;">Jaipur, Rajasthan</p>
        <p style="font-size: 0.9rem; color: var(--muted-gold);">By appointment only</p>
      </div>
    </div>

    <!-- CONTACT FORM -->
    <div class="contact-form-section">
      <h2>Send us a Message</h2>
      <form id="contactForm">
        <div class="form-grid">
          <div class="form-group">
            <input type="text" id="name" placeholder="Your Name" required />
          </div>
          <div class="form-group">
            <input type="email" id="email" placeholder="Your Email" required />
          </div>
          <div class="form-group">
            <input type="tel" id="phone" placeholder="Phone (Optional)" />
          </div>
          <div class="form-group">
            <select id="subject" required>
              <option value="">Select Subject</option>
              <option value="order">Order Issue</option>
              <option value="auction">Auction Question</option>
              <option value="custom">Custom Order</option>
              <option value="return">Return/Exchange</option>
              <option value="other">Other</option>
            </select>
          </div>
        </div>
        <div class="form-group">
          <textarea id="message" placeholder="Tell us about your Snehvastra journey..." required></textarea>
        </div>
        <button type="submit" class="submit-btn">✨ Send Message</button>
      </form>
    </div>

    <!-- STUDIO LOCATION -->
    <div style="text-align: center; margin-top: 60px;">
      <h3 style="font-family: 'Clash Display', serif; margin-bottom: 24px;">Our Jaipur Studio</h3>
      <iframe class="studio-map" src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d22774.876625999998!2d75.857725!3d26.912434!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x396db47d7d9e8c9d%3A0x75e1e4f6c4e2e4f6!2sJaipur%2C%20Rajasthan!5e0!3m2!1sen!2sin!4v1732900000000" allowfullscreen="" loading="lazy"></iframe>
    </div>
  </main>

  <!-- WHATSAPP FLOAT -->
  <a href="https://wa.me/917597152209?text=Hi%20Snehvastra!" class="whatsapp-float" target="_blank">💬</a>

  <footer>
    © <span id="currentYear"></span> Snehvastra. All Rights Reserved. •
    <span style="opacity: 0.85;">Instagram: <strong>@sneh_vastra</strong></span>
  </footer>

  <!-- TOAST -->
  <div id="toast" class="toast"></div>

  <script>
    // MASTER SCRIPT + CONTACT FUNCTIONALITY
    let cart = JSON.parse(localStorage.getItem('snehvastra_cart')) || {};
    
    function updateCartCount() {
      const totalItems = Object.values(cart).reduce((sum, qty) => sum + qty, 0);
      document.getElementById('cartCount').textContent = totalItems;
    }

    function showToast(message, type = 'success') {
      const toast = document.getElementById('toast');
      toast.textContent = message;
      toast.className = `toast ${type}`;
      toast.classList.add('show');
      setTimeout(() => toast.classList.remove('show'), 4000);
    }

    // CONTACT FORM
    document.getElementById('contactForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      const formData = new FormData(e.target);
      const data = Object.fromEntries(formData);
      
      // Simulate form submission
      document.querySelector('.submit-btn').textContent = 'Sending...';
      document.querySelector('.submit-btn').disabled = true;
      
      setTimeout(() => {
        showToast('✅ Message sent! We\'ll reply within 24 hours.', 'success');
        e.target.reset();
        document.querySelector('.submit-btn').textContent = '✨ Send Message';
        document.querySelector('.submit-btn').disabled = false;
      }, 1500);
      
      // Real implementation: send to your backend
      // fetch('/api/contact', { method: 'POST', body: formData });
    });

    // AUCTION TIMER
    const auctionDate = new Date('2025-12-01T18:00:00').getTime();
    setInterval(() => {
      const now = new Date().getTime();
      const diff = auctionDate - now;
      const timer = document.getElementById('auctionTimer');
      if (timer && diff > 0) {
        const d = Math.floor(diff / 86400000);
        const h = Math.floor((diff % 86400000) / 3600000);
        const m = Math.floor((diff % 3600000) / 60000);
        const s = Math.floor((diff % 60000) / 1000);
        timer.textContent = `${d}d ${h}h ${m}m ${s}s`;
      } else if (timer) {
        timer.textContent = 'LIVE NOW!';
      }
    }, 1000);

    window.addEventListener('load', () => {
      updateCartCount();
      document.getElementById('currentYear').textContent = new Date().getFullYear();
    });
  </script>

  <!-- Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-CXQG6L0W01"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-CXQG6L0W01');
  </script>
</body>
</html>
